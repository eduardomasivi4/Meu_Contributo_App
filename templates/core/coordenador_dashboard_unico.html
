{% extends 'base.html' %}
{% load static %}

{% block content %}
<div class="container">
    <div class="cabecalho">
        <h2>
            <i class="fas {% if tipo == 'cultural' %}fa-palette{% else %}fa-flask{% endif %}"></i>
            Painel do Coordenador
            {% if tipo == 'cultural' %}
                <span class="badge-cultural">Actividades Culturais</span>
            {% else %}
                <span class="badge-ciencia">Actividades Ciência e Tecnologia</span>
            {% endif %}
        </h2>
        <div class="user-info">
            <span>{{ request.user.get_full_name|default:request.user.username }}</span>
            <button id="logoutBtn" class="btn-sair">Sair</button>
        </div>
    </div>

    <div class="acoes">
        <a href="{% url 'coordenador_criar_atividade_separado' %}?tipo={{ tipo }}" class="btn-criar">
            <i class="fas fa-plus"></i> Criar Actividade
        </a>
    </div>

    <div class="search-bar">
        <i class="fas fa-search"></i>
        <input type="text" id="searchInput" placeholder="Pesquisar actividades..." value="{{ search_query }}">
        <button id="searchBtn">Buscar</button>
        <button id="clearSearchBtn">Limpar</button>
    </div>

    <div class="atividades-grid">
        {% for atividade in atividades %}
        <div class="atividade-card">
            <div class="atividade-header {% if tipo == 'cultural' %}cultural{% else %}ciencia{% endif %}">
                <span class="atividade-tipo">
                    <i class="fas {% if tipo == 'cultural' %}fa-palette{% else %}fa-flask{% endif %}"></i>
                    {% if tipo == 'cultural' %}Cultural{% else %}Ciência e Tecnologia{% endif %}
                </span>
                <h3>{{ atividade.nome }}</h3>
            </div>
            <div class="atividade-body">
                <p><i class="fas fa-calendar"></i> Criada: {{ atividade.created_at|date:"d/m/Y" }}</p>
                <p><i class="fas fa-graduation-cap"></i> {{ atividade.get_cursos_display }}</p>
                <div class="status">
                    {% if atividade.finalizada %}
                        <span class="badge-finalizada">Finalizada</span>
                    {% endif %}
                    {% if atividade.distribuida %}
                        <span class="badge-distribuida">Pontos Distribuídos</span>
                    {% elif atividade.finalizada %}
                        <span class="badge-pendente">Aguardando Distribuição</span>
                    {% endif %}
                </div>
                <div class="acoes-card">
                    {% if not atividade.finalizada %}
                        <a href="{% url 'coordenador_editar_atividade' atividade.id %}" class="btn-editar">Editar</a>
                        <button class="btn-eliminar" data-id="{{ atividade.id }}" data-nome="{{ atividade.nome }}">Eliminar</button>
                    {% else %}
                        <a href="{% url 'coordenador_ver_registos' atividade.id %}" class="btn-ver">Ver Registos</a>
                    {% endif %}
                </div>
            </div>
        </div>
        {% empty %}
        <div class="sem-atividades">
            <i class="fas fa-calendar-times" style="font-size: 48px; color: #D4A373;"></i>
            <h3>Nenhuma actividade encontrada</h3>
            <p>Crie sua primeira actividade clicando no botão acima.</p>
        </div>
        {% endfor %}
    </div>

    {% if atividades.has_other_pages %}
    <div class="pagination">
        {% if atividades.has_previous %}
            <a href="?page={{ atividades.previous_page_number }}{% if search_query %}&search={{ search_query }}{% endif %}">« Anterior</a>
        {% endif %}
        <span class="current">Página {{ atividades.number }} de {{ atividades.paginator.num_pages }}</span>
        {% if atividades.has_next %}
            <a href="?page={{ atividades.next_page_number }}{% if search_query %}&search={{ search_query }}{% endif %}">Próxima »</a>
        {% endif %}
    </div>
    {% endif %}
</div>

<style>
    .cabecalho {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
        flex-wrap: wrap;
    }
    .badge-cultural {
        background: #D4A373;
        color: white;
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 12px;
        margin-left: 10px;
    }
    .badge-ciencia {
        background: #5C3A21;
        color: white;
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 12px;
        margin-left: 10px;
    }
    .btn-sair {
        background: #D9534F;
        color: white;
        border: none;
        padding: 6px 12px;
        border-radius: 20px;
        cursor: pointer;
    }
    .acoes { margin-bottom: 20px; }
    .btn-criar {
        background: #2B7A4B;
        color: white;
        padding: 10px 20px;
        border-radius: 30px;
        text-decoration: none;
        display: inline-block;
    }
    .search-bar {
        background: white;
        border-radius: 16px;
        padding: 12px 20px;
        margin-bottom: 25px;
        display: flex;
        align-items: center;
        gap: 15px;
        flex-wrap: wrap;
        border: 1px solid #F5EDDA;
    }
    .search-bar input { flex: 1; border: none; outline: none; padding: 8px 0; }
    .search-bar button { background: #2B7A4B; color: white; border: none; padding: 8px 20px; border-radius: 30px; cursor: pointer; }
    .atividades-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
        gap: 20px;
    }
    .atividade-card {
        background: white;
        border-radius: 16px;
        overflow: hidden;
        border: 1px solid #F5EDDA;
    }
    .atividade-header {
        padding: 15px;
        color: white;
    }
    .atividade-header.cultural { background: linear-gradient(135deg, #D4A373, #B5835A); }
    .atividade-header.ciencia { background: linear-gradient(135deg, #5C3A21, #7A5233); }
    .atividade-body { padding: 15px; }
    .status { margin: 10px 0; display: flex; gap: 8px; flex-wrap: wrap; }
    .badge-finalizada { background: #E9F5EF; color: #2B7A4B; padding: 4px 8px; border-radius: 20px; font-size: 11px; }
    .badge-distribuida { background: #D4A373; color: white; padding: 4px 8px; border-radius: 20px; font-size: 11px; }
    .badge-pendente { background: #FEF3C7; color: #B45309; padding: 4px 8px; border-radius: 20px; font-size: 11px; }
    .acoes-card { margin-top: 15px; display: flex; gap: 10px; }
    .btn-editar, .btn-ver { background: #D4A373; color: white; padding: 5px 10px; border-radius: 20px; text-decoration: none; font-size: 12px; }
    .btn-eliminar { background: #D9534F; color: white; border: none; padding: 5px 10px; border-radius: 20px; cursor: pointer; font-size: 12px; }
    .pagination {
        display: flex;
        justify-content: center;
        gap: 10px;
        margin-top: 30px;
    }
    .pagination a, .pagination span {
        padding: 8px 16px;
        background: white;
        border-radius: 20px;
        text-decoration: none;
        color: #5C3A21;
    }
    .pagination .current {
        background: #2B7A4B;
        color: white;
    }
    .sem-atividades {
        text-align: center;
        padding: 60px;
        background: white;
        border-radius: 20px;
        grid-column: 1/-1;
    }
</style>

<script>
    function getCookie(name) {
        let value = null;
        if (document.cookie && document.cookie !== '') {
            const cookies = document.cookie.split(';');
            for (let cookie of cookies) {
                cookie = cookie.trim();
                if (cookie.substring(0, name.length + 1) === (name + '=')) {
                    value = decodeURIComponent(cookie.substring(name.length + 1));
                    break;
                }
            }
        }
        return value;
    }

    // Pesquisa
    document.getElementById('searchBtn')?.addEventListener('click', () => {
        const search = document.getElementById('searchInput').value;
        window.location.href = `?search=${encodeURIComponent(search)}`;
    });
    document.getElementById('clearSearchBtn')?.addEventListener('click', () => {
        window.location.href = window.location.pathname + `?tipo={{ tipo }}`;
    });
    document.getElementById('searchInput')?.addEventListener('keypress', (e) => {
        if (e.key === 'Enter') document.getElementById('searchBtn').click();
    });

    // Eliminar actividade
    document.querySelectorAll('.btn-eliminar').forEach(btn => {
        btn.addEventListener('click', async function() {
            const id = this.dataset.id;
            const nome = this.dataset.nome;
            if (confirm(`Tem certeza que deseja eliminar "${nome}"?`)) {
                const response = await fetch(`/coordenador/atividade/eliminar/${id}/`, {
                    method: 'POST',
                    headers: { 'X-CSRFToken': getCookie('csrftoken') }
                });
                if (response.ok) location.reload();
            }
        });
    });

    // Logout
    document.getElementById('logoutBtn')?.addEventListener('click', () => {
        window.location.href = "{% url 'index' %}";
    });
</script>
{% endblock %}